# Copy to `.env` or `.env.local` for local dev. Names only in `dflow.template.json` — no secrets here.
# Vite inlines `VITE_*` into the client bundle; never put API keys or tokens in these variables.

VITE_APP_TITLE=dFlow React (Vite) starter
